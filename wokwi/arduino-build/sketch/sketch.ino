// Arduino CLI requires the primary .ino file to match its directory name.
// Keep the Wokwi-required production source at ../../sketch.ino and include
// it here so both build paths compile exactly the same firmware logic.
#include "../../sketch.ino"
